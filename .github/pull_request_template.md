# PR

## What changed

## Why

## Validation

- [ ] `npm run validate` passes
- [ ] Updated `docs/` if behavior changed
- [ ] No absolute paths, no machine-specific references

## Skill checklist (if touching `skills/`)

- [ ] `name` matches folder, `description` non-empty
- [ ] Every relative link and backticked skill/agent ref resolves
- [ ] Chat Output Contract ends with single next step
